//for in loop

//objects

const lang = {
  js: "javaScript",
  cpp: "c++",
  rb: "ruby",
};

for (const key in lang) {
  console.log(lang[key]);
}

//array

const array = ["javaScript", "c++", "ruby"];

for (const key in array) {
  console.log(array[key]);
}
